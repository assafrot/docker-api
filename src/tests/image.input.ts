const name = "my-image1"
const repository = "my-repo"
const version = "1.1.0"
const metadata = {
    key: "value",
    key2: 1
}

export const imageInput = {
    name: name,
    repository: repository,
    version: version,
    metadata: metadata,
}
